---
phase: 04-public-trust-surfaces
plan: 06
type: execute
wave: 4
depends_on:
  - 04-05
files_modified:
  - supabase/migrations/20260410000000_phase4_slug_aliases.sql
autonomous: false
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
  - SET-03
  - SET-04
  - UX-03
  - UX-04

must_haves:
  truths:
    - "All tests pass including Wave 0 tests"
    - "TypeScript compiles with zero errors"
    - "Supabase migration applied successfully"
    - "Production deployment on Vercel succeeds"
    - "Public pages work on production URL without authentication"
  artifacts:
    - path: "supabase/migrations/20260410000000_phase4_slug_aliases.sql"
      provides: "Slug alias table deployed to production Supabase"
  key_links:
    - from: "Vercel deployment"
      to: "Supabase production"
      via: "Database migration applied"
      pattern: "document_slug_aliases"
---

<objective>
Apply Supabase migration, run full test suite, deploy to Vercel production, and verify all Phase 4 features work on the live production URL.

Purpose: Ship Phase 4 to production and verify the full public trust surfaces experience end-to-end.
Output: Production deployment verified on invios.online.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-trust-surfaces/04-CONTEXT.md
@.planning/phases/04-public-trust-surfaces/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and apply Supabase migration</name>
  <files>supabase/migrations/20260410000000_phase4_slug_aliases.sql</files>
  <read_first>
    - supabase/migrations/20260410000000_phase4_slug_aliases.sql (migration to apply)
  </read_first>
  <action>
**Step 1: Run full test suite**
```bash
pnpm test && pnpm typecheck
```
Both must pass with zero failures.

**Step 2: Apply Supabase migration to production**

Use the Supabase CLI to push the migration to the linked production project (project ref: `kwprfrplurkjztedglaf` per reference_project_ids.md):

```bash
npx supabase db push
```

If the migration fails, diagnose and fix the SQL. Common issues: table already exists (add `IF NOT EXISTS`), permission errors (check service role).

**Step 3: Verify migration applied**

```bash
npx supabase db dump --schema public | grep document_slug_aliases
```

Confirm the table and index exist in the remote schema.

**Step 4: Deploy to Vercel production**

```bash
npx vercel --prod
```

Wait for deployment to complete. Note the deployment URL.
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV && pnpm test && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits with 0 (all tests pass)
    - `pnpm typecheck` exits with 0 (no type errors)
    - Supabase migration applied (document_slug_aliases table exists in production)
    - Vercel deployment succeeds (deployment URL returned)
  </acceptance_criteria>
  <done>Full test suite green, migration applied, production deployment completed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Production verification of Phase 4 features</name>
  <files>supabase/migrations/20260410000000_phase4_slug_aliases.sql</files>
  <action>
Human verifies complete Phase 4 deployed to production: public invoice/quotation pages with branding, client portal, accept/reject quotations, slug-based URLs, bilingual/RTL rendering, TRN display, and UX-03 visual polish.

Steps:
1. Open invios.online (or Vercel deployment URL) and sign in
2. Navigate to the dashboard — verify visual polish (consistent spacing, metric cards, no raw elements)
3. Navigate to an invoice detail page — verify URL uses slug format (not UUID)
4. Copy a share link from ShareModal — open in incognito
5. Verify public invoice page: branded header, status badge, centered preview, Download PDF button, "Powered by Invios" footer
6. Create or find a quotation in "sent" status, copy share link
7. Open public quotation page in incognito — verify Accept/Reject buttons
8. Click Accept on one quotation — verify status updates
9. Click Reject on another — verify textarea appears, enter a note, confirm
10. Navigate to a client detail page, copy the portal URL
11. Open portal in incognito — verify branded header, document list, links work
12. If bilingual/Arabic documents exist: verify side-by-side layout and RTL rendering
13. If a tax invoice exists: verify TRN displayed in document header
14. Test on mobile — verify public pages and portal are usable
  </action>
  <verify>Human confirms all Phase 4 features work on production URL</verify>
  <done>All Phase 4 requirements verified on production: PUB-01..05, SET-03, SET-04, UX-03, UX-04 confirmed working on invios.online.</done>
</task>

</tasks>

<verification>
- Full test suite passes
- TypeScript compiles cleanly
- Production deployment accessible
- All Phase 4 requirements verified on production
</verification>

<success_criteria>
- Public invoice and quotation links work without authentication and look professional (PUB-01, PUB-02, PUB-03)
- Client portal links show correct document set (PUB-04)
- Accept/reject works on public quotation page (PUB-05)
- TRN and AED support rendered correctly (SET-03)
- Bilingual EN/AR and RTL layout work (SET-04)
- Premium visual quality across all views (UX-03)
- Slug-based URLs with alias redirects (UX-04)
- All deployed to production on invios.online
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-trust-surfaces/04-06-SUMMARY.md`
</output>
