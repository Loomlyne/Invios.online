---
phase: 05-automation-recovery
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/actions/invoices.test.ts
  - src/lib/billing-data.test.ts
  - src/lib/cron-utils.test.ts
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05]

must_haves:
  truths:
    - "RED test stubs exist for version snapshot on save (AUTO-01)"
    - "RED test stubs exist for restore action (AUTO-02)"
    - "RED test stubs exist for recurring schedule date advancement (AUTO-03)"
    - "RED test stubs exist for reminder send logic and deduplication (AUTO-04, AUTO-05)"
  artifacts:
    - path: "src/actions/invoices.test.ts"
      provides: "RED stubs for snapshotInvoiceVersion and restoreInvoiceVersionAction"
      contains: "describe(\"snapshotInvoiceVersion\""
    - path: "src/lib/cron-utils.test.ts"
      provides: "RED stubs for advanceNextDueDate and shouldSendReminder"
      contains: "describe(\"advanceNextDueDate\""
    - path: "src/lib/billing-data.test.ts"
      provides: "RED stubs for listInvoiceVersions and getRecurringSchedule"
      contains: "describe(\"listInvoiceVersions\""
  key_links: []
---

<objective>
Create Wave 0 RED test scaffolds for all Phase 5 pure-logic requirements.

Purpose: Establish failing tests that implementation plans will turn GREEN. Tests define behavioral contracts before code exists.
Output: Three test files with RED stubs covering AUTO-01 through AUTO-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-automation-recovery/05-CONTEXT.md
@.planning/phases/05-automation-recovery/05-RESEARCH.md

@src/actions/invoices.test.ts
@src/lib/billing-data.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED test stubs for version snapshot + restore (AUTO-01, AUTO-02)</name>
  <files>src/actions/invoices.test.ts</files>
  <read_first>
    src/actions/invoices.test.ts
    src/actions/invoices.ts
    src/lib/billing-data.ts
  </read_first>
  <action>
Append new describe blocks to the existing `src/actions/invoices.test.ts` file. Do NOT modify existing tests.

Add the following RED test stubs (they will fail because the functions do not exist yet):

**describe("snapshotInvoiceVersion"):**
- Test: "creates a version snapshot with correct JSONB shape" — import `snapshotInvoiceVersion` from `@/actions/versions` (does not exist yet). Call with a mock supabase client, invoiceId "inv-1", userId "user-1", and a snapshot object containing `{ invoice_number: "INV-001", client_id: "c-1", client_name: "Acme", total: 5000, line_items: [], issue_date: "2026-04-12", due_date: "2026-04-26", currency: "AED", tax_rate: 5, discount: 0, subtotal: 4762, discount_amount: 0, tax_amount: 238, notes: "", terms: "", language: "en", trn: "", invoice_type: "invoice" }`. Assert `supabase.from("invoice_versions").insert` was called with matching invoice_id, user_id, snapshot fields.
- Test: "enforces rolling 10-cap by deleting oldest versions" — Mock supabase to return 11 version rows from select query (ordered by created_at desc). Assert `supabase.from("invoice_versions").delete().in("id", [oldest_id])` was called with the 11th row's id.
- Test: "does not delete when under 10 versions" — Mock supabase to return 5 version rows. Assert delete was NOT called.

**describe("restoreInvoiceVersionAction"):**
- Test: "applies snapshot fields to invoice and recomputes status" — import `restoreInvoiceVersionAction` from `@/actions/versions`. Mock requireSession, mock supabase select to return a snapshot, assert `supabase.from("invoices").update()` was called with snapshot fields, and assert `computeAndWriteInvoiceStatus` was called.
- Test: "returns error when version not found" — Mock supabase select to return null. Assert returned ActionState has `status: "error"`.

All tests should use `vi.mock` for `@/lib/require-session`, `@/lib/supabase/server`, and `@/lib/billing-data` (for computeAndWriteInvoiceStatus). Use static imports with `vi.mock()` hoisting pattern (per project convention — no dynamic imports).

Each test body should call the function and assert. They will fail at import time since `@/actions/versions` does not exist — that is the expected RED state. Wrap each test in `it.todo("...")` or use `it("...", () => { expect(true).toBe(false); })` as placeholder RED markers. Use `it.todo` for cleaner output.
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV && pnpm test src/actions/invoices.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/invoices.test.ts contains `describe("snapshotInvoiceVersion"`
    - src/actions/invoices.test.ts contains `describe("restoreInvoiceVersionAction"`
    - src/actions/invoices.test.ts contains at least 5 new it.todo or failing it() calls
    - Existing invoiceFormSchema and invoiceStatuses tests still pass
    - pnpm test exits with pending/todo markers for the new stubs (not hard failures that break the suite)
  </acceptance_criteria>
  <done>5 RED test stubs for AUTO-01 and AUTO-02 appended to invoices.test.ts, existing tests unbroken</done>
</task>

<task type="auto">
  <name>Task 2: RED test stubs for cron utilities (AUTO-03, AUTO-04, AUTO-05)</name>
  <files>src/lib/cron-utils.test.ts, src/lib/billing-data.test.ts</files>
  <read_first>
    src/lib/billing-data.test.ts
    .planning/phases/05-automation-recovery/05-RESEARCH.md
  </read_first>
  <action>
**Create new file `src/lib/cron-utils.test.ts`** with RED test stubs:

```typescript
import { describe, it, expect } from "vitest";
```

**describe("advanceNextDueDate"):**
- it.todo: "advances weekly by 7 days" — advanceNextDueDate("2026-04-12", "weekly") should return "2026-04-19"
- it.todo: "advances monthly by 1 month" — advanceNextDueDate("2026-04-12", "monthly") should return "2026-05-12"
- it.todo: "advances quarterly by 3 months" — advanceNextDueDate("2026-04-12", "quarterly") should return "2026-07-12"
- it.todo: "handles month-end rollover (Jan 31 + monthly = Feb 28)" — advanceNextDueDate("2026-01-31", "monthly") should return "2026-02-28"

**describe("shouldSendReminder"):**
- it.todo: "returns 'before' when days until due matches reminder_days_before" — shouldSendReminder({ dueDate: "2026-04-15", today: "2026-04-12", reminderDaysBefore: 3, reminderDaysAfter: 7, remindOnDueDate: true, secondReminderDays: 14 }) should return "before"
- it.todo: "returns 'due_date' when today equals due date and remindOnDueDate is true" — shouldSendReminder with today == dueDate should return "due_date"
- it.todo: "returns 'after' when days past due matches reminder_days_after" — today is 7 days after due_date with reminderDaysAfter: 7 should return "after"
- it.todo: "returns 'second' when days past due matches second_reminder_days"
- it.todo: "returns null when no timing rule matches"
- it.todo: "returns null when remindOnDueDate is false and today equals due date"

**Append to existing `src/lib/billing-data.test.ts`** (do NOT modify existing tests):

**describe("listInvoiceVersions"):**
- it.todo: "returns versions for a given invoice ordered by created_at desc"

**describe("getRecurringSchedule"):**
- it.todo: "returns active recurring schedule for a given invoice"
- it.todo: "returns null when no schedule exists"

All stubs use `it.todo(...)` pattern. These will show as pending in Vitest output, not failures.
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV && pnpm test src/lib/cron-utils.test.ts src/lib/billing-data.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/cron-utils.test.ts exists and contains `describe("advanceNextDueDate"`
    - src/lib/cron-utils.test.ts contains `describe("shouldSendReminder"`
    - src/lib/cron-utils.test.ts contains at least 10 it.todo calls
    - src/lib/billing-data.test.ts contains `describe("listInvoiceVersions"`
    - src/lib/billing-data.test.ts contains `describe("getRecurringSchedule"`
    - Existing billing-data tests still pass
    - pnpm test exits 0 (todo tests are pending, not failures)
  </acceptance_criteria>
  <done>10+ RED test stubs for AUTO-03/04/05 created across two test files, existing tests unbroken</done>
</task>

</tasks>

<verification>
pnpm test --reporter=verbose — all existing tests pass, new stubs show as pending/todo
</verification>

<success_criteria>
- 15+ RED test stubs across 3 files covering all 5 AUTO requirements
- Zero regressions in existing test suite
- Each AUTO requirement has at least 2 test stubs
</success_criteria>

<output>
After completion, create `.planning/phases/05-automation-recovery/05-00-SUMMARY.md`
</output>
