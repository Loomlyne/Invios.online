---
phase: 05-automation-recovery
plan: 06
type: execute
wave: 4
depends_on: [05-02, 05-03, 05-04, 05-05]
files_modified:
  - supabase/migrations/20260412000000_phase5_automation.sql
autonomous: false
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04, AUTO-05]

user_setup:
  - service: vercel
    why: "CRON_SECRET env var required for cron job authentication"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a random secret (e.g., openssl rand -hex 32) and add to Vercel Environment Variables"
    dashboard_config:
      - task: "Add CRON_SECRET environment variable"
        location: "Vercel Dashboard -> Project Settings -> Environment Variables"

must_haves:
  truths:
    - "All Phase 5 tests pass (existing + new)"
    - "Supabase migration applied successfully"
    - "CRON_SECRET env var set in Vercel"
    - "Production deploy succeeds on Vercel"
    - "Invoice version history visible on invoice detail page"
    - "Cron endpoints return 401 without valid secret"
  artifacts:
    - path: "supabase/migrations/20260412000000_phase5_automation.sql"
      provides: "Applied migration for 3 new tables"
  key_links:
    - from: "vercel.json"
      to: "src/app/api/cron/recurring/route.ts"
      via: "cron schedule declaration"
      pattern: "/api/cron/recurring"
    - from: "vercel.json"
      to: "src/app/api/cron/reminders/route.ts"
      via: "cron schedule declaration"
      pattern: "/api/cron/reminders"
---

<objective>
Run full test suite, apply Supabase migration, deploy to Vercel production, and verify all Phase 5 features end-to-end.

Purpose: Ship Phase 5 to production with verified database schema, working cron endpoints, and user-facing version history.
Output: Production deployment with all Phase 5 features live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-automation-recovery/05-CONTEXT.md

@vercel.json
@supabase/migrations/20260412000000_phase5_automation.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + Supabase migration + Vercel deploy</name>
  <files>supabase/migrations/20260412000000_phase5_automation.sql</files>
  <read_first>
    supabase/migrations/20260412000000_phase5_automation.sql
    vercel.json
    src/lib/env.ts
  </read_first>
  <action>
**1. Run full test suite:**
```bash
pnpm test --reporter=verbose
```
All tests must pass. If any fail, fix the failing tests before proceeding.

**2. Apply Supabase migration:**
```bash
npx supabase db push --linked
```
This applies the Phase 5 migration to the linked Supabase project (kwprfrplurkjztedglaf). The migration creates:
- `invoice_versions` table with RLS
- `recurring_schedules` table with RLS
- `reminder_logs` table with RLS
- Reminder settings columns on `user_settings` (IF NOT EXISTS — safe for already-live columns)

If `db push` fails, check the error. Common issues:
- Table already exists: The `IF NOT EXISTS` guards handle this.
- Column already exists: The `ADD COLUMN IF NOT EXISTS` guards handle this.
- Permission errors: Ensure the service role key is configured.

**3. Generate CRON_SECRET and set in Vercel:**
```bash
# Generate a secure random secret
CRON_SECRET=$(openssl rand -hex 32)
echo "CRON_SECRET=$CRON_SECRET"

# Set in Vercel (production only — cron only runs on production)
npx vercel env add CRON_SECRET production <<< "$CRON_SECRET"
```

Also add `CRON_SECRET` to `.env.local` for local testing:
```bash
echo "CRON_SECRET=$CRON_SECRET" >> .env.local
```

**4. Deploy to Vercel production:**
```bash
npx vercel --prod --yes
```

**5. Verify cron endpoints are registered:**
After deploy, check Vercel dashboard or run:
```bash
npx vercel crons ls 2>/dev/null || echo "Check Vercel dashboard manually for cron registration"
```

**6. Verify cron auth guard works:**
```bash
# Should return 401 — no valid secret
curl -s -o /dev/null -w "%{http_code}" https://invios.online/api/cron/recurring
# Expected: 401

# Should return 200 — valid secret (test in local dev)
curl -s -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/recurring
```
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV && pnpm test --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test passes with 0 failures
    - Supabase migration applied (db push succeeds or tables already exist)
    - CRON_SECRET env var set in Vercel production environment
    - Vercel production deploy succeeds
    - /api/cron/recurring returns 401 without Authorization header
    - /api/cron/reminders returns 401 without Authorization header
  </acceptance_criteria>
  <done>Full test suite green, migration applied, CRON_SECRET configured, production deployed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 5 features in production</name>
  <files>none</files>
  <action>
Present the verification checklist to the user. All automation is complete — this task verifies the user-facing experience.

Phase 5 Automation and Recovery features built:
1. Invoice version history — automatic snapshots on every save, collapsible panel on invoice detail page, restore with warn+confirm dialog
2. Recurring billing — "Repeat this invoice" toggle in builder, "Make recurring" button on detail page, daily cron creates draft copies
3. Reminder automation — daily cron sends branded reminder emails based on configured timing rules, dedup via reminder_logs table
  </action>
  <verify>
**Version History (AUTO-01, AUTO-02):**
1. Go to https://invios.online/app/invoices
2. Open any existing invoice and click "Edit"
3. Change a line item amount or date and save
4. Return to the invoice detail page
5. Look for the "Version History" section below expenses — it should show "1 saved version"
6. Click to expand — verify the version row shows the date/time and previous total amount
7. Edit and save the invoice again — version count should increase to 2
8. Click "Restore version" on the older version
9. Verify the restore dialog shows current vs restored totals
10. Confirm the restore — invoice fields should revert to the older version

**Recurring Billing (AUTO-03):**
11. Create a new invoice — look for "Repeat this invoice" toggle below Notes/Terms
12. Enable it — frequency pills (Weekly/Monthly/Quarterly) and date picker should appear
13. Select "Monthly" and save the invoice
14. On the invoice detail page, verify "Recurring: Monthly" button appears in the actions row
15. Click it to verify the edit dialog opens with current settings

**Reminder System (AUTO-04, AUTO-05):**
16. Go to Settings > Notifications — verify reminder settings are still there and functional
17. (Cron verification is manual — wait for next daily run or check Vercel cron logs)

**Cron Security:**
18. Visit https://invios.online/api/cron/recurring directly in browser — should see "Unauthorized" (401)
19. Visit https://invios.online/api/cron/reminders directly in browser — should see "Unauthorized" (401)
  </verify>
  <done>User approves all Phase 5 features or describes issues to fix</done>
</task>

</tasks>

<verification>
pnpm test — all tests pass
Vercel deploy succeeds
Manual verification of version history, recurring config, and cron security
</verification>

<success_criteria>
- All Phase 5 features verified in production
- Version history panel works end-to-end (save, view, restore)
- Recurring billing toggle and configuration work
- Cron endpoints are secured with CRON_SECRET
- Reminder settings still functional
- Zero regressions in existing features
</success_criteria>

<output>
After completion, create `.planning/phases/05-automation-recovery/05-06-SUMMARY.md`
</output>
