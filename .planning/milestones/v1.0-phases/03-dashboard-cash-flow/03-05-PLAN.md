---
phase: 03-dashboard-cash-flow
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, UX-02]

must_haves:
  truths:
    - "All Phase 3 features work end-to-end in production"
    - "Dashboard shows live financial data from real invoices and payments"
    - "Payment recording updates invoice status automatically"
    - "Overdue invoices appear on dashboard when past due date"
    - "Vercel production deployment is live and functional"
  artifacts: []
  key_links: []
---

<objective>
Run the Supabase migration, verify the full Phase 3 feature set works end-to-end (dashboard metrics, payment/expense recording, status automation, profit display), deploy to Vercel production, and get human sign-off.

Purpose: Phase completion gate. Ensures all Phase 3 requirements work in production before marking the phase done.
Output: Live production deployment with working dashboard and payment flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-cash-flow/03-CONTEXT.md
@.planning/phases/03-dashboard-cash-flow/03-03-SUMMARY.md
@.planning/phases/03-dashboard-cash-flow/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Supabase migration, run full test suite, deploy to Vercel</name>
  <files></files>
  <read_first>
    supabase/migrations/20260407120000_phase3_payments_expenses.sql,
    .planning/phases/03-dashboard-cash-flow/03-CONTEXT.md
  </read_first>
  <action>
**Step 1 -- Apply Supabase migration:**
Run `npx supabase db push` to apply the Phase 3 migration (creates payments + expenses tables with RLS).

If `supabase db push` is not available or fails, apply the migration directly via the Supabase dashboard SQL editor by executing the contents of `supabase/migrations/20260407120000_phase3_payments_expenses.sql`.

Verify tables exist: `npx supabase db reset --dry-run` or check via Supabase dashboard that `payments` and `expenses` tables exist with RLS enabled.

**Step 2 -- Run the full test suite:**
Run `pnpm test` to verify all unit tests pass (existing Phase 1/2 tests + new Phase 3 tests).

**Step 3 -- Build check:**
Run `pnpm build` to verify the production build succeeds without errors.

**Step 4 -- Deploy to Vercel production:**
Run `npx vercel --prod` to deploy to production.

Record the deployment URL.

**Step 5 -- Verify deployment:**
Check that the deployment URL returns a 200 status on the dashboard route.
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (all tests pass)
    - `pnpm build` exits 0 (production build succeeds)
    - Supabase migration applied (payments and expenses tables exist)
    - Vercel deployment succeeds and returns a production URL
  </acceptance_criteria>
  <done>Migration applied, all tests pass, production build succeeds, Vercel deployment live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 3 features in production</name>
  <files></files>
  <action>
Present the production deployment URL to the user and walk them through the verification checklist below. Wait for human approval or issue report.
  </action>
  <verify>Human types "approved" or reports issues</verify>
  <done>Human confirms all Phase 3 features work in production.</done>
  <what-built>
    Complete Phase 3: Dashboard and Cash Flow implementation including:
    1. Dashboard with financial metrics (billed, collected, outstanding, collection rate)
    2. Quick actions strip (new invoice, quotation, client)
    3. Recent invoices + quotations side-by-side
    4. Overdue invoices section (when applicable)
    5. Payment recording on invoice detail page (inline ledger table)
    6. Expense recording on invoice detail page (inline ledger table)
    7. Profit and margin display per invoice
    8. Automatic invoice status updates (paid, partial_paid, overdue)
    9. Empty states for new users
  </what-built>
  <how-to-verify>
    **Dashboard verification:**
    1. Open the production URL then navigate to /app (dashboard)
    2. Verify 4 metric cards show (Total billed, Collected, Outstanding, Collection rate)
    3. Verify quick action buttons exist (New invoice, New quotation, New client)
    4. Verify recent invoices and quotations columns render (or empty states if no data)
    5. If you have a new account with no data: metrics show em dashes, empty state copy shows

    **Payment flow:**
    6. Navigate to an existing invoice detail page (/app/invoices/[id])
    7. Scroll down to see Profit summary, Payments table, Expenses table
    8. In the Payments add row: enter a date, amount, and method then click "Add payment"
    9. Verify the payment appears in the list
    10. Verify the invoice status badge updates (e.g., to "partial_paid" or "paid")
    11. Delete the payment row then verify it disappears and status reverts

    **Expense flow:**
    12. In the Expenses add row: enter date, amount, description, vendor then click "Add expense"
    13. Verify the expense appears in the list
    14. Verify profit and margin update to reflect the expense
    15. Delete the expense then verify profit reverts

    **Overdue test:**
    16. If you have a sent invoice with a past due date, check that the dashboard shows the Overdue section

    **Mobile:**
    17. Resize to mobile width then verify metric cards go to 2 columns, recent docs stack, date columns hide in ledger tables
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 3 complete, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Supabase migration applied successfully
- Full test suite passes
- Production build succeeds
- Vercel deployment live
- Human verifies all Phase 3 features work
</verification>

<success_criteria>
- All 10 Phase 3 requirements (DASH-01..04, OPS-01..05, UX-02) verified working in production
- Human approves the deployment
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-cash-flow/03-05-SUMMARY.md`
</output>
