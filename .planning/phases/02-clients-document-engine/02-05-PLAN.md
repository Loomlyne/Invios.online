---
phase: 02-clients-document-engine
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
requirements:
  - CLNT-01
  - CLNT-02
  - CLNT-03
  - CLNT-04
  - QUOT-01
  - QUOT-02
  - QUOT-03
  - QUOT-04
  - QUOT-05
  - QUOT-06
  - QUOT-07
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - INV-05
  - INV-06
  - INV-07
  - INV-08

must_haves:
  truths:
    - "Full product loop works: create client, build quotation, mark sent, accept, convert to invoice, mark sent, share, PDF download"
    - "Builder never shows a status dropdown"
    - "Converted quotation Edit button is disabled"
    - "Share modal copies public URL to clipboard"
    - "Client detail shows status badges on document rows"
    - "Application builds and deploys to Vercel"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 product loop end-to-end and deploy to Vercel production.

Purpose: Phase 2 is not done until the full loop is verified working: create a client, build a quotation, advance its status, convert it to an invoice, share it, download PDF, and verify all UI decisions (D-01 through D-09) are correctly implemented. Then deploy to Vercel and verify live.
Output: Verified and deployed Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-clients-document-engine/02-CONTEXT.md
@.planning/phases/02-clients-document-engine/02-01-SUMMARY.md
@.planning/phases/02-clients-document-engine/02-02-SUMMARY.md
@.planning/phases/02-clients-document-engine/02-03-SUMMARY.md
@.planning/phases/02-clients-document-engine/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build</name>
  <files></files>
  <read_first>
    - .planning/phases/02-clients-document-engine/02-VALIDATION.md
  </read_first>
  <action>
Run the full validation suite to confirm no regressions:

1. `pnpm test --run` -- all unit tests pass
2. `pnpm build` -- TypeScript compiles, Next.js build succeeds
3. Verify no type errors in any modified file

If any test fails, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV && pnpm test --run && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --run` exits with code 0
    - `pnpm build` exits with code 0
    - No TypeScript errors in output
  </acceptance_criteria>
  <done>Full test suite green, build succeeds with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 2 product loop</name>
  <files></files>
  <action>
Present the human verification checklist to the user. Start the dev server with `pnpm dev` if not already running.

Complete Phase 2 implementation includes:
- Builder status dropdown removed (D-04), replaced with read-only badge
- ShareModal component (D-01) with clipboard copy
- DocumentStatusActions component (D-03) with contextual buttons
- DocumentSummaryRow component (D-08) with status badges
- Invoice detail: ShareModal, DocumentStatusActions, PDF download
- Quotation detail: ShareModal, DocumentStatusActions, D-07 lock
- Client detail: DocumentSummaryRow for document listings
- Quotation-to-invoice conversion redirects to builder (D-06)
- PDF routes have 60s maxDuration

Verification steps for the user:

**Flow 1 -- Client + Quotation lifecycle:**
1. Go to /app/clients, click "Add client", create a test client
2. Click into client detail page, verify metric cards show 0 invoices, 0 quotations
3. Click "New quotation", verify builder has NO status dropdown (just a badge showing "Draft")
4. Fill in line items, save, verify redirect to quotation detail page (D-05)
5. On detail page: click "Mark as sent", status badge changes to "Sent"
6. Click "Mark as accepted", status badge changes to "Accepted"
7. Click "Convert to invoice", verify redirect to invoice BUILDER (edit page) not detail page (D-06)
8. Go back to quotation detail, verify Edit button is DISABLED with "Converted" badge and tooltip (D-07)

**Flow 2 -- Invoice lifecycle:**
9. On the invoice builder (from step 7), verify line items are pre-filled from quotation
10. Save the invoice, verify redirect to invoice detail page
11. On detail page: click "Mark as sent", status badge changes to "Sent"

**Flow 3 -- Share + PDF:**
12. On invoice detail: click "Share", verify ShareModal opens with public URL
13. Click "Copy link", verify button changes to "Copied" then reverts
14. Click "Open in new tab", verify public invoice page loads
15. Click "PDF", verify PDF file downloads

**Flow 4 -- Client detail summary:**
16. Go to client detail, verify document rows show status badges (not ArrowRight icons)
17. Verify metric cards show correct billed total and quoted total

Wait for user response: "approved" or issues to fix.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human approval"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 4 flows work correctly
    - User types "approved" to proceed
  </acceptance_criteria>
  <done>Human has verified the full Phase 2 product loop works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy to Vercel production</name>
  <files></files>
  <read_first></read_first>
  <action>
Deploy the current state to Vercel production:

```bash
vercel --prod --yes
```

After deployment completes, verify the live URL loads without errors by checking the deployment output for the production URL.
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV && vercel --prod --yes 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Vercel deployment succeeds (exit code 0)
    - Production URL is printed in output
    - No build errors in deployment output
  </acceptance_criteria>
  <done>Phase 2 is deployed to Vercel production and accessible at the live URL.</done>
</task>

</tasks>

<verification>
The human verification checkpoint covers the full product loop. Automated verification covers build + tests.
</verification>

<success_criteria>
- All tests pass, build succeeds
- Human has verified the full product loop (create client, quotation lifecycle, invoice conversion, share, PDF, client detail badges)
- Application is deployed to Vercel production
</success_criteria>

<output>
After completion, create `.planning/phases/02-clients-document-engine/02-05-SUMMARY.md`
</output>
