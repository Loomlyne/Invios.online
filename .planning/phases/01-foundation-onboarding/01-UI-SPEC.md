---
phase: 1
slug: foundation-onboarding
status: draft
shadcn_initialized: true
preset: stone / css-variables / rsc / tsx
created: 2026-04-06
source: reverse-engineered from implemented codebase
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Foundation & Onboarding.
> Generated by gsd-ui-researcher from the implemented codebase. All decisions locked from source code — not hypothesized.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | style: default, baseColor: stone, cssVariables: true, rsc: true, tsx: true |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide React |
| Font — body | DM Sans (`--font-sans`, variable from `next/font/google`) |
| Font — display | Cormorant Garamond (`--font-display`, weights 500/600/700, variable from `next/font/google`) |
| CSS config | `src/app/globals.css` — custom CSS properties mapped through `@theme inline` for Tailwind v4 |

---

## Color Tokens

All tokens live in `:root` in `src/app/globals.css` and are forwarded to `@theme inline` for Tailwind utility generation.

| Token | Raw Value | Tailwind class |
|-------|-----------|----------------|
| `--background` | `#f8f4ee` | `bg-background` |
| `--foreground` | `#17120f` | `text-foreground` |
| `--surface` | `#fffdf9` | `bg-surface` |
| `--surface-strong` | `#f1e9dc` | `bg-surface-strong` |
| `--border` | `rgba(28,25,23,0.12)` | `border-border` |
| `--muted` | `#6b6359` | `text-muted` |
| `--muted-strong` | `#3a322c` | `text-muted-strong` |
| `--accent` | `#ca8a04` | `text-accent`, `bg-accent` |
| `--accent-strong` | `#8f6510` | `text-accent-strong` |
| `--accent-soft` | `rgba(202,138,4,0.12)` | `bg-accent-soft` |
| `--success` | `#24583a` | `text-success` |
| `--danger` | `#8d3d2e` | `text-danger` |
| `--on-dark` | `#fff9f0` | `.text-on-dark` (always-emitted utility) |

### Global Background

The `body` uses a layered gradient — not a flat color — applied both in CSS and with a mobile override at `max-width: 768px`:

```
Desktop: radial-gradient(top-left accent glow 16%) + radial-gradient(top-right dark whisper 10%) + linear-gradient(180deg, #fdfbf8 → #f8f4ee → #f4eee3)
Mobile: radial-gradient(top-center accent glow 14%) + linear-gradient(180deg, #fdfbf8 → #f4eee3)
```

---

## Color Contract (60/30/10)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f8f4ee` background gradient | Page background, main content area |
| Secondary (30%) | `#fffdf9` surface / `rgba(255,253,249,0.82)` glass panels | Cards (`bg-white/84`), sidebar glass, header glass, auth panels |
| Accent (10%) | `#ca8a04` amber-gold | Reserved for: primary CTA buttons, active nav item icon, progress bar fill, focus ring, brand color field, selection highlight |
| Destructive | `#8d3d2e` | `Button variant="danger"` only — no other usage in Phase 1 |
| Dark surface | `#1c1917` | Auth left panel background only |

Accent reserved for: primary CTA button (`variant="accent"`), active sidebar nav icon, setup progress bar fill, `::selection` highlight, `:focus-visible` outline, accent badge variant. Never used for body text, borders, or passive UI elements.

---

## Spacing Scale

The project uses Tailwind's default spacing scale (4px base unit). These are the values observed in the codebase for Phase 1 surfaces:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`gap-1`, `p-1`) | Step indicator number spacing |
| sm | 8px (`gap-2`, `p-2`) | Nav item gaps, icon button padding |
| md | 16px (`gap-4`, `p-4`) | Form field gaps, card internal padding, header px |
| lg | 24px (`gap-6`, `p-6`) | Card padding default, section gaps |
| xl | 32px (`gap-8`) | Auth layout column gap |
| 2xl | 48px | Auth left panel `mt-14` heading offset (56px, nearest standard) |
| 3xl | 64px | — |

Exceptions:
- Touch targets: icon-only buttons (`size-10` = 40px, `size-11` = 44px for icon Button size). Settings gear and sign-out use `size-10`.
- App shell bottom padding on mobile: `pb-28` (112px) to clear mobile nav bar.
- Auth left panel heading: `mt-14` (56px) from brand wordmark to h1.
- Onboarding overlay: `px-3 py-4` on mobile, `px-6 py-6` on md+.
- Mobile nav chip horizontal padding: `px-3` (12px) — tight fit for nav label at minimum readable width. 12px is a multiple of 4 but not in the standard set; justified by constrained chip width.

---

## Typography

| Role | Family | Size | Weight | Line Height | Class pattern |
|------|--------|------|--------|-------------|---------------|
| Label / UI / Eyebrow | DM Sans | 12px | 400–500 | 1.25 | `text-xs` / `text-xs uppercase tracking-[0.24–0.28em]` |
| Body | DM Sans | 14px | 400 / 600 | 1.5 (leading-6) / 1.75 (leading-7) | `text-sm` / `text-sm leading-7` / `text-sm font-semibold` |
| Card title | DM Sans | 20px | 600 | 1.3 | `text-xl font-semibold tracking-tight` |
| Page heading | Cormorant Garamond | responsive: 30px → 36px | 600 | tight | `display-text text-3xl sm:text-4xl font-semibold tracking-tight` |

Notes on the heading scale:
- The auth display (`text-4xl sm:text-5xl` = 36px → 48px) is a responsive variant of the page heading entry, not a separate scale step. Both live in the 30–48px range and use the same `.display-text` utility + weight 600. The auth form title (`text-4xl` = 36px) is the fixed-width expression of the same entry.
- The `.display-text` utility class applies `font-family: var(--font-display)` + `letter-spacing: -0.03em`.

Active font weights in use: **400** (regular) and **600** (semibold). Weight 500 is available on Cormorant Garamond (loaded) but not actively used in Phase 1 UI.

---

## Radius System

Custom radius tokens (not Tailwind defaults):

| Token | Value | Usage |
|-------|-------|-------|
| `--radius-sm` | `0.85rem` (13.6px) | — |
| `--radius-md` | `1.1rem` (17.6px) | Card list items, nav chips, step pills |
| `--radius-lg` | `1.75rem` (28px) | — |
| Inline: `rounded-[1rem]` | `16px` | Form inputs, inline feedback banners, checklist items |
| Inline: `rounded-[1.1rem]` | `17.6px` | Quick action cards, dashboard nav tiles |
| Inline: `rounded-[1.15rem]` | `18.4px` | Desktop sidebar nav links |
| Inline: `rounded-[1.2rem]` | `19.2px` | Onboarding preview confirm card |
| Inline: `rounded-[1.25rem]` | `20px` | EmptyState container |
| Inline: `rounded-[1.3rem]` | `20.8px` | Onboarding confirm review card |
| Inline: `rounded-[1.4rem]` | `22.4px` | App shell sticky header |
| Inline: `rounded-[1.45rem]` | `23.2px` | SetupChecklist floating panel |
| Inline: `rounded-[1.6rem]` | `25.6px` | Card base component |
| Inline: `rounded-[1.8rem]` | `28.8px` | Desktop sidebar glass panel |
| Inline: `rounded-[2rem]` | `32px` | Auth panels (both columns) |
| `rounded-full` | pill | Buttons (all variants), badge, nav chips on mobile, step indicators |

Rule: all interactive containers use large organic radii (16–32px). Only text, icons, and pure decorative elements are radius-free.

---

## Shadow System

| Token class | Value | Usage |
|-------------|-------|-------|
| `.soft-shadow` | `0 32px 80px rgba(21,17,13,0.16)` | Auth panels |
| `.subtle-shadow` | `0 18px 48px rgba(21,17,13,0.12)` | App shell sidebar + header, Card base |
| Button accent shadow | `0 18px 45px rgba(202,138,4,0.24)` | `variant="accent"` buttons |
| Button primary shadow | `0 18px 45px rgba(28,25,23,0.18)` | `variant="primary"` buttons |
| Button danger shadow | `0 18px 45px rgba(141,61,46,0.22)` | `variant="danger"` buttons |
| SetupChecklist | `0 24px 60px rgba(23,18,15,0.18)` | Floating bottom-right panel |

---

## Glass / Backdrop Pattern

The `.glass-panel` utility: `backdrop-filter: blur(18px)` + `background: rgba(255,253,249,0.82)`. Applied to the sticky app header and the desktop sidebar nav panel. Not used in onboarding overlay (which uses `bg-[#140F0B]/72 backdrop-blur-sm` instead).

---

## Layout Structure

### App Shell

```
max-width: 1600px, mx-auto, px-4 py-4 (sm: px-6, lg: px-8 py-6)
├── Sidebar (hidden on < lg): w-64, sticky top-6, glass-panel, rounded-[1.8rem]
│   ├── Wordmark: display-text text-2xl font-semibold (Cormorant Garamond)
│   └── AppSidebarNav (desktop mode, flex-col gap-2)
└── Main column (flex-1, min-w-0)
    ├── Header (glass-panel sticky top-4 z-20, rounded-[1.4rem], px-4 py-3)
    │   ├── Business name (text-sm font-semibold, truncate)
    │   ├── "New invoice" CTA (Button variant="accent" size="sm")
    │   ├── Settings gear (size-10, rounded-full, border, bg-white/80, aria-label="Open settings")
    │   ├── SignOutButton (variant="ghost" size="sm")
    │   └── AppSidebarNav (mobile mode, horizontal scroll chips)
    └── main (PageTransition wraps children)
        └── pb-28 on mobile (for bottom nav clearance)
```

### Auth Layout

```
max-width: 1400px, mx-auto, min-h-screen
Grid: lg:grid-cols-[0.85fr_1.15fr] gap-8 px-4 py-6 (lg: px-8)
├── Left panel: bg-[#1C1917], rounded-[2rem], soft-shadow, overflow-hidden
│   ├── Radial gradient overlay (accent top-left 28%, white highlight bottom-right 30%)
│   ├── Brand wordmark (display-text text-2xl font-semibold, text-[#F8F4EE])
│   ├── h1 display heading (display-text text-4xl sm:text-5xl, text-[#F8F4EE])
│   ├── Description (text-sm leading-7, text-[#DDD4C6])
│   ├── Feature list (text-sm leading-7, text-[#DDD4C6])
│   └── Bottom CTA link (text-[#F7CB6A], ArrowRight icon)
└── Right panel: items-center justify-center
    └── Form card: max-w-lg, rounded-[2rem], bg-white/88, soft-shadow, p-6 sm:p-8
        ├── Eyebrow: text-xs uppercase tracking-[0.28em] text-muted → "Auth"
        ├── Form title: display-text text-4xl font-semibold
        ├── Description: text-sm leading-7 text-muted
        └── Form fields + submit Button (variant="accent" size="lg" w-full)
```

### Onboarding Overlay

```
fixed inset-0 z-40, bg-[#140F0B]/72, backdrop-blur-sm, px-3 py-4 (md: px-6 py-6)
└── Grid: max-w-[1380px] gap-4, lg:grid-cols-[minmax(0,1fr)_minmax(360px,540px)]
    ├── Form card: Card component, bg-[#F9F5EE], overflow-hidden, p-0
    │   ├── Header bar: px-5 py-4 (sm: px-6), border-b border-black/8
    │   │   ├── "Setup" heading + mobile "Preview" trigger button
    │   │   └── Step pills: rounded-full px-3 py-2 text-sm
    │   │       Active: bg-foreground/8 font-medium text-foreground
    │   │       Done: Check icon text-success
    │   │       Pending: number badge bg-foreground/10 text-muted
    │   └── Form content: max-h-[calc(100vh-12rem)] overflow-y-auto px-5 py-5 (sm: px-6)
    └── Preview panel: hidden lg:block, sticky top-[5.5rem], max-h-[calc(100vh-7rem)] overflow-y-auto
```

### Settings Layout

```
Grid: gap-6 xl:grid-cols-[minmax(0,1fr)_480px]
├── Card with Tabs (Profile / Branding / Defaults)
│   └── Preview button (Dialog trigger) visible on < xl only
└── Preview panel: hidden xl:block, sticky top-[7rem]
```

---

## Navigation

### Desktop Sidebar Nav

- Container: `flex flex-col gap-2` inside glass panel
- Link: `rounded-[1.15rem] px-4 py-3 text-sm transition`
- Active state: `bg-[#17120F] text-on-dark shadow-[0_16px_40px_rgba(23,18,15,0.16)]`
- Inactive: `text-muted-strong hover:bg-black/5`
- Icon: `size-4`, active color `text-accent`, inactive `text-muted`
- Label: `font-medium`

### Mobile Nav (horizontal scroll chips in header)

> **Note — D-16 deviation:** CONTEXT.md decision D-16 specifies "bottom-tab primary nav." The implemented design uses horizontal scroll chips inside the sticky top header instead of a bottom tab bar. This is a deliberate deviation from D-16. The executor must preserve the horizontal chip pattern as implemented — do not introduce a bottom tab bar.

- Container: `flex gap-2 overflow-x-auto pb-1` (no bottom tab bar — chips sit inside sticky header)
- Chip: `rounded-full px-3 py-2 text-sm transition`
- Active: `bg-foreground/8 font-medium text-foreground`
- Inactive: `text-muted-strong hover:bg-foreground/4`
- Icon: `size-3.5`, active `text-accent-strong`, inactive `text-muted`

Nav items (locked from `appNavItems` constant): Dashboard, Invoices, Quotations, Clients, Settings.

---

## Component Inventory

### Button

| Variant | Background | Text | Shadow | Hover |
|---------|-----------|------|--------|-------|
| `primary` | `bg-foreground` (#17120f) | `text-on-dark` | `0 18px 45px rgba(28,25,23,0.18)` | `-translate-y-0.5`, bg `#2B2623` |
| `secondary` | `bg-surface` + `border-border` | `text-foreground` | none | border `#CAB9A2`, bg `#FFF7EA` |
| `ghost` | transparent | `text-foreground` | none | border `black/8`, bg `black/5` |
| `inverse` | `bg-white/10` + `border-white/12` | `text-on-dark` | `0 16px 36px rgba(12,10,9,0.18)` | border `white/16`, bg `white/16` |
| `accent` | `bg-accent` (#ca8a04) | `text-[#1C1917]` | `0 18px 45px rgba(202,138,4,0.24)` | `-translate-y-0.5`, bg `#DEA325` |
| `danger` | `bg-[#8D3D2E]` | `text-white` | `0 18px 45px rgba(141,61,46,0.22)` | bg `#7b3124` |

Sizes: `sm` h-10 px-4 | `md` h-12 px-6 (default) | `lg` h-14 px-7 text-base | `icon` size-11 rounded-full.

All buttons: `rounded-full`, `text-sm font-medium`, `transition-all`, `focus-visible:ring-2 ring-[#CA8A04]/45`.

### Input

`h-12 w-full rounded-[1rem] border border-border bg-white/85 px-4 text-sm` + `shadow-[0_1px_0_rgba(255,255,255,0.6)_inset]` + `focus-visible:border-accent`.

Color input exception: `h-14 p-2` (used for brand color pickers).

### Textarea

Same base as Input but auto-height, `min-h-[110px]` in onboarding forms.

### Card

`rounded-[1.6rem] border border-border bg-white/84 p-6 subtle-shadow`.

Subcomponents: `CardHeader` (flex flex-col gap-2), `CardTitle` (text-xl font-semibold tracking-tight), `CardDescription` (text-sm text-muted), `CardContent` (mt-5).

### Badge

`inline-flex items-center rounded-full border px-3 py-1 text-xs font-medium tracking-[0.16em] uppercase`

Variants: `default` (bg-[#FFF7EA], border-[#E3D6C2]), `accent` (bg-accent-soft, border-accent/30, text-accent-strong), `success` (bg-emerald-50, border-emerald-900/20, text-success), `warning` (bg-amber-50, border-amber-700/20, text-amber-800).

### EmptyState

`rounded-[1.25rem] border border-border bg-surface px-6 py-10 text-center`. Icon in `size-10 rounded-full bg-surface-strong`. Title `text-base font-semibold`. Description `text-sm leading-7 text-muted`. Actions `flex flex-wrap justify-center gap-3 mt-5`.

### PageHeader

`flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between`. Title: `display-text text-3xl sm:text-4xl font-semibold tracking-tight`. Description: `text-sm leading-7 text-muted mt-2`.

### StatStrip

`flex flex-wrap items-baseline gap-x-4 gap-y-1 text-sm mt-3`. Label: `text-muted`. Value: `font-semibold text-foreground`. Separator dot: `text-muted/30`.

### SetupChecklist (floating)

`fixed bottom-4 right-4 z-40 w-[calc(100vw-2rem)] max-w-sm lg:bottom-6 lg:right-6`. Panel: `rounded-[1.45rem] border border-black/10 bg-white/96 shadow-[0_24px_60px_rgba(23,18,15,0.18)] backdrop-blur`. Progress bar: `h-2 rounded-full bg-[#F3EBDD]` track, `bg-accent` fill with `transition-[width] duration-300`.

---

## Animation / Motion

### Page Transitions (GSAP)

`PageTransition` component wraps `main` content. On route change: `gsap.from` with `opacity: 0`, `y: 16`, `duration: 0.38`, `ease: "power3.out"`, `clearProps: "opacity,transform"`.

### CSS Keyframes

| Name | Effect | Duration | Easing |
|------|--------|----------|--------|
| `animate-enter` | `opacity 0→1`, `translateY 18px→0` | 420ms | `cubic-bezier(0.22,1,0.36,1)` |
| `animate-glow` | `translate3d + scale(1.02)` shift | 6s | `ease-in-out infinite` |
| `animate-pulse` (Tailwind) | Loading skeletons | default | — |
| `animate-spin` (Tailwind) | Loader2 icon during pending states | default | — |

### Reduced Motion

Full `prefers-reduced-motion: reduce` block in globals.css forces all `animation-duration: 1ms`, `transition-duration: 1ms`. GSAP also checks `window.matchMedia("(prefers-reduced-motion: reduce)")` and skips animation when true.

---

## Loading / Skeleton States

Loading skeletons use `animate-pulse` with `bg-black/8` (darker) and `bg-black/6` (lighter) rounded-full shapes. Pattern: match the shape of the content being loaded. See `src/app/(app)/app/loading.tsx` for the dashboard skeleton pattern.

---

## State Feedback Patterns

### Inline form feedback

`rounded-[1rem] border px-4 py-3 text-sm`

| State | Border | Background | Text |
|-------|--------|------------|------|
| Error | `border-[#E7B1A8]` | `bg-[#FFF3F1]` | `text-[#8D3D2E]` |
| Success | `border-emerald-900/10` | `bg-emerald-50` | `text-success` |
| Neutral | `border-black/8` | `bg-white` | `text-muted-strong` |

### Warning banner (onboarding preview step)

`rounded-[1.2rem] border border-amber-700/10 bg-amber-50 px-4 py-4 text-sm text-amber-900`

Used for the "skipped logo/signature — unbranded-draft warning" message.

### Progress indicator

`h-2 overflow-hidden rounded-full bg-[#F3EBDD]` track with `bg-accent` fill and `transition-[width] duration-300`.

### Pending / loading in buttons

`Loader2 className="size-4 animate-spin"` prepended to button label. Button gains `disabled` attribute. Used consistently across all form submit paths.

### Checklist items

Complete: `border-[#DCEBDD] bg-[#F6FBF6]` — success green tint.
Pending: `border-border bg-[#FFFCF7] hover:border-[#D7C4A7] hover:bg-[#FFF8ED]` — warm hover.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Sign-in CTA | "Enter Invios" |
| Sign-up CTA | "Create account" |
| Sign-in page headline | "Return to the billing console." |
| Sign-up page headline | "Create the workspace before the first invoice." |
| Auth eyebrow | "Auth" (uppercase, tracked) |
| App shell — new document CTA | "New invoice" |
| Onboarding step 1 title | "Business identity and contact layer" |
| Onboarding step 1 description | "This is the non-negotiable setup that makes documents trustworthy." |
| Onboarding step 2 title | "Branding can start now and finish later" |
| Onboarding step 2 description | "Logo and signature can evolve. The billing shell should already feel premium." |
| Onboarding step 3 title | "Document defaults that unblock the builder later" |
| Onboarding step 3 description | "These are required before the app can safely move into real document creation." |
| Onboarding step 4 title | "Preview and finish" |
| Onboarding step 4 description | "The real builder is Phase 2. This confirms the shell is brand-ready enough to continue." |
| Onboarding step 1 CTA | "Save and continue" |
| Onboarding step 2 CTA | "Save branding" |
| Onboarding step 2 skip | "Skip for now" |
| Onboarding step 3 CTA | "Save defaults" |
| Onboarding step 4 CTA | "Finish setup" |
| Onboarding step 4 secondary | "Review branding" |
| Onboarding unbranded warning | "If you skipped logo or signature, Invios will still continue. Document flows will surface an unbranded-draft warning until you finish them in settings." |
| Settings save profile CTA | "Save profile" |
| Settings save branding CTA | "Save branding" |
| Settings save defaults CTA | "Save defaults" |
| Dashboard empty (setup incomplete) | "Finish [next item] to complete setup." |
| Dashboard CTA (setup incomplete) | "Finish [next item]" + ArrowRight |
| Dashboard CTA (setup complete) | "Open invoices" + ArrowRight |
| Setup checklist title (in progress) | "Setup checklist" |
| Setup checklist title (auto-completing) | "Finalizing setup" |
| Setup checklist title (complete) | "Setup complete" |
| Setup checklist dismiss CTA | "Dismiss checklist" |
| SetupChecklist secondary CTA | "Open setup settings" |
| Install prompt (ready) | "Install Invios" |
| Install prompt (accepted) | "Installed" |
| Sign out | "Sign out" |
| Auth left panel feature list | "Branded invoices and quotations" / "Public share links and PDF export" / "UAE-ready defaults out of the box" |
| Auth left panel bottom | "Already configured? Open the console →" |
| Sign-up footer | "Need a reset instead? Reset your password" |
| Config missing gate title | "Add Supabase before the private shell can run." |

Empty state heading pattern: `[Surface] is empty` — e.g. "No invoices yet". Description adds the next step action.

Error state pattern: `[What went wrong]. [What to do next].` — e.g. "Email already in use. Sign in instead or use a different address."

No destructive actions exist in Phase 1 (no delete flows, no irreversible operations). The `danger` button variant is available but not triggered.

---

## Signature Capture (Phase 1 exclusive)

Four modes selectable via pill toggle:

| Mode | Label | Interaction |
|------|-------|-------------|
| `none` | "Add later" | No capture UI — onboarding continues |
| `upload` | "Upload" | `<input type="file" accept="image/*">` |
| `draw` | "Draw" | `<SignaturePad>` canvas component |
| `typed` | "Typed" | Text input + font select (Signature / Cormorant Garamond / DM Sans) |

Mode toggle style: `rounded-[1rem] border px-4 py-3 text-sm font-medium capitalize`. Active: `border-foreground bg-foreground text-background`. Inactive: `border-border bg-white hover:bg-[#FFF7EA]`.

---

## Mobile Behavior

- Breakpoint system: Tailwind's `sm` (640px), `md` (768px), `lg` (1024px), `xl` (1280px)
- Below `lg`: sidebar hidden, nav becomes horizontal scroll chips inside sticky header
- Below `lg`: onboarding preview panel hidden — exposed via "Preview" button that opens `<Dialog>`
- Below `xl`: settings preview panel hidden — same Dialog pattern
- Below `md`: onboarding overlay uses `px-3 py-4`; above md: `px-6 py-6`
- Mobile body padding-bottom `pb-28` in main column (prevents content under floating elements)
- Signature pad and form grids collapse from `sm:grid-cols-2` to single column below 640px

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, input, textarea, label, card, badge, tabs, dialog | not required |
| No third-party registries | — | not applicable |

All components in `src/components/ui/` are locally owned implementations (not fetched from third-party registries). They extend or replace shadcn defaults with project-specific styling.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
