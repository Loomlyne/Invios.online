---
phase: 08-settings-foundation
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/components/app/settings/settings-shell.tsx
  - src/components/app/settings/panels/profile-panel.tsx
  - src/components/app/settings/panels/branding-panel.tsx
  - src/components/app/settings/panels/business-info-panel.tsx
  - src/components/app/settings/panels/general-panel.tsx
  - src/components/app/settings/panels/emails-panel.tsx
  - src/components/app/settings/panels/integrations-panel.tsx
  - src/components/app/settings/panels/billing-panel.tsx
  - src/lib/constants.ts
autonomous: false
requirements: [NAV-04, A11Y-01, A11Y-03]

must_haves:
  truths:
    - "User on mobile can open a section picker drawer and switch sections"
    - "Each section renders a placeholder panel with its own disabled SaveButton"
    - "Integrations and Billing panels show 'Coming soon' cards without Save buttons"
    - "Branding nav item in appNavItems points to /app/settings?section=branding"
  artifacts:
    - path: "src/components/app/settings/panels/profile-panel.tsx"
      provides: "Placeholder profile panel with disabled SaveButton"
      exports: ["ProfilePanel"]
    - path: "src/components/app/settings/panels/integrations-panel.tsx"
      provides: "Coming soon card for integrations"
      exports: ["IntegrationsPanel"]
    - path: "src/components/app/settings/panels/billing-panel.tsx"
      provides: "Coming soon card for billing"
      exports: ["BillingPanel"]
    - path: "src/components/app/settings/settings-shell.tsx"
      provides: "Updated shell with mobile picker and panel rendering"
      contains: "Drawer"
  key_links:
    - from: "src/components/app/settings/settings-shell.tsx"
      to: "src/components/app/settings/panels/profile-panel.tsx"
      via: "conditional render on activeSection"
      pattern: 'activeSection.*===.*"profile"'
    - from: "src/components/app/settings/settings-shell.tsx"
      to: "vaul"
      via: "Drawer import for mobile picker"
      pattern: "from.*vaul"
    - from: "src/lib/constants.ts"
      to: "/app/settings?section=branding"
      via: "appNavItems branding href"
      pattern: "settings\\?section=branding"
---

<objective>
Complete the settings foundation: add the mobile section picker (Vaul Drawer), create all 7 placeholder panels, wire panel rendering into the shell, and update the app nav so Branding points to the new settings section.

Purpose: With this plan complete, every settings section is navigable on both desktop and mobile, all panels are placeholder-ready for Phases 9-12, and the Branding nav entry is updated.
Output: 7 panel files, updated shell with mobile picker, updated constants.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-settings-foundation/08-CONTEXT.md
@.planning/phases/08-settings-foundation/08-RESEARCH.md
@.planning/phases/08-settings-foundation/08-UI-SPEC.md
@.planning/phases/08-settings-foundation/08-01-SUMMARY.md
@.planning/phases/08-settings-foundation/08-02-SUMMARY.md

@src/lib/types.ts
@src/lib/constants.ts
@src/components/app/settings/settings-shell.tsx
@src/components/app/settings/settings-sidebar.tsx

<interfaces>
<!-- From Plan 01 and Plan 02 outputs -->

From src/components/app/settings/shared/save-button.tsx:
```typescript
export function SaveButton({ isDirty, onSave }: { isDirty: boolean; onSave: () => Promise<void> }): JSX.Element;
```

From src/components/app/settings/shared/settings-section.tsx:
```typescript
export function Section({ title, description, danger, children }: Props): JSX.Element;
export function Field({ label, htmlFor, children }: Props): JSX.Element;
```

From src/components/app/settings/settings-sidebar.tsx:
```typescript
export const SIDEBAR_ITEMS: SidebarItem[];  // { key, label, Icon }
export function SettingsSidebar({ active, onNavigate }: Props): JSX.Element;
```

From src/lib/types.ts:
```typescript
export type SettingsSection = "profile" | "branding" | "business" | "general" | "emails" | "integrations" | "billing";
export interface AppContext { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Placeholder panels and nav update</name>
  <files>
    src/components/app/settings/panels/profile-panel.tsx,
    src/components/app/settings/panels/branding-panel.tsx,
    src/components/app/settings/panels/business-info-panel.tsx,
    src/components/app/settings/panels/general-panel.tsx,
    src/components/app/settings/panels/emails-panel.tsx,
    src/components/app/settings/panels/integrations-panel.tsx,
    src/components/app/settings/panels/billing-panel.tsx,
    src/lib/constants.ts
  </files>
  <read_first>
    - src/components/app/settings/shared/settings-section.tsx (Section component API)
    - src/components/app/settings/shared/save-button.tsx (SaveButton component API)
    - src/lib/constants.ts (appNavItems array — branding entry at index 4 with href "/app/branding")
    - src/components/app/app-sidebar-nav.tsx (iconMap — currently includes "palette" for branding)
    - .planning/phases/08-settings-foundation/08-UI-SPEC.md (Copywriting Contract section for exact placeholder text and Coming Soon copy)
  </read_first>
  <action>
Create directory `src/components/app/settings/panels/`.

**Panels 1-5 (placeholder panels with SaveButton):**

Each of the 5 content panels follows this identical pattern. Create one file per panel:

- `profile-panel.tsx` — title: "Profile", description: "Profile settings will be available here."
- `branding-panel.tsx` — title: "Branding", description: "Branding settings will be available here."
- `business-info-panel.tsx` — title: "Business Info", description: "Business Info settings will be available here."
- `general-panel.tsx` — title: "General", description: "General settings will be available here."
- `emails-panel.tsx` — title: "Emails", description: "Emails settings will be available here."

Template for each (replace TITLE and DESCRIPTION):
```typescript
"use client";

import { Section } from "../shared/settings-section";
import { SaveButton } from "../shared/save-button";

export function ProfilePanel() {
  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <div>
          <h2 className="text-lg font-semibold">Profile</h2>
          <p className="text-sm text-muted mt-1">Manage your personal information and preferences.</p>
        </div>
        <div className="hidden lg:block">
          <SaveButton isDirty={false} onSave={async () => {}} />
        </div>
      </div>
      <Section title="Profile" description="Profile settings will be available here.">
        <p className="text-sm text-muted">Content will be added in a future phase.</p>
      </Section>
      {/* Mobile sticky save */}
      <div className="lg:hidden sticky bottom-16 bg-surface border-t border-border px-4 py-3">
        <SaveButton isDirty={false} onSave={async () => {}} />
      </div>
    </div>
  );
}
```

Key details:
- Each panel exports a named function (e.g., `export function ProfilePanel()`)
- SaveButton is always `isDirty={false}` (no wired actions in Phase 8 — per Pitfall 5)
- `onSave` is `async () => {}` — a no-op async function
- Desktop: SaveButton in header row (`hidden lg:block`)
- Mobile: SaveButton in sticky bottom bar (`lg:hidden sticky bottom-16`)
- Per A11Y-03: each panel has its OWN SaveButton — no shared save state
- No `context` prop needed for placeholders — context wiring happens in Phases 9-12

**Panels 6-7 (Coming Soon — no SaveButton):**

`integrations-panel.tsx`:
```typescript
"use client";

import { Plug } from "lucide-react";

export function IntegrationsPanel() {
  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-lg font-semibold">Integrations</h2>
        <p className="text-sm text-muted mt-1">Connect with third-party services.</p>
      </div>
      <div className="rounded-[var(--radius-md)] border border-border bg-white p-8 text-center">
        <Plug className="size-8 text-muted mx-auto mb-3" />
        <p className="text-sm font-medium">Integrations</p>
        <p className="text-sm text-muted mt-1">Third-party integrations are coming soon.</p>
      </div>
    </div>
  );
}
```

`billing-panel.tsx`:
```typescript
"use client";

import { CreditCard } from "lucide-react";

export function BillingPanel() {
  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-lg font-semibold">Billing</h2>
        <p className="text-sm text-muted mt-1">Manage your plan and payment details.</p>
      </div>
      <div className="rounded-[var(--radius-md)] border border-border bg-white p-8 text-center">
        <CreditCard className="size-8 text-muted mx-auto mb-3" />
        <p className="text-sm font-medium">Billing</p>
        <p className="text-sm text-muted mt-1">Plan and billing management is coming soon.</p>
      </div>
    </div>
  );
}
```

Key: No SaveButton in Coming Soon panels — they have no editable content.

**constants.ts update:**

In `src/lib/constants.ts`, update the `appNavItems` array's branding entry to point to the settings branding section instead of the standalone page:

Change the branding entry:
```typescript
{
  key: "branding",
  label: "Branding",
  href: "/app/settings?section=branding" as unknown as import("next").Route,
  icon: "palette",
  description: "Logo, colors, fonts, and document identity.",
},
```

This updates the desktop sidebar nav link. The `/app/branding` standalone page continues to exist until Phase 9 adds the redirect (per RESEARCH.md Open Question 2 recommendation).
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV/.claude/worktrees/jolly-euler && ls src/components/app/settings/panels/*.tsx | wc -l && grep "coming soon" src/components/app/settings/panels/integrations-panel.tsx && grep "settings?section=branding" src/lib/constants.ts</automated>
  </verify>
  <acceptance_criteria>
    - 7 panel files exist in src/components/app/settings/panels/
    - profile-panel.tsx exports `ProfilePanel` function
    - branding-panel.tsx exports `BrandingPanel` function
    - business-info-panel.tsx exports `BusinessInfoPanel` function
    - general-panel.tsx exports `GeneralPanel` function
    - emails-panel.tsx exports `EmailsPanel` function
    - integrations-panel.tsx exports `IntegrationsPanel` function
    - billing-panel.tsx exports `BillingPanel` function
    - All 5 content panels (profile through emails) contain `SaveButton` with `isDirty={false}`
    - integrations-panel.tsx contains `"Third-party integrations are coming soon."` (exact copy from UI-SPEC)
    - billing-panel.tsx contains `"Plan and billing management is coming soon."` (exact copy from UI-SPEC)
    - integrations-panel.tsx does NOT contain SaveButton
    - billing-panel.tsx does NOT contain SaveButton
    - src/lib/constants.ts branding entry href is `"/app/settings?section=branding"`
  </acceptance_criteria>
  <done>All 7 panel stubs created — 5 with disabled SaveButton, 2 with "Coming soon" cards; branding nav item updated to point to settings section</done>
</task>

<task type="auto">
  <name>Task 2: Mobile section picker and panel wiring in shell</name>
  <files>src/components/app/settings/settings-shell.tsx</files>
  <read_first>
    - src/components/app/settings/settings-shell.tsx (current shell from Plan 02 — placeholder panel area to be replaced)
    - src/components/app/settings/settings-sidebar.tsx (SIDEBAR_ITEMS export — reuse for mobile picker items)
    - node_modules/vaul/dist/index.d.mts OR node_modules/vaul/dist/index.js (check actual vaul export API — Drawer.Root vs named exports; RESEARCH.md flagged this as LOW confidence)
    - .planning/phases/08-settings-foundation/08-UI-SPEC.md (Mobile Section Picker section — trigger classes, drawer styling)
    - .planning/phases/08-settings-foundation/08-RESEARCH.md (Pattern 5: Mobile Section Picker)
  </read_first>
  <action>
Update `src/components/app/settings/settings-shell.tsx` to:
1. Add mobile section picker using Vaul Drawer (NAV-04)
2. Wire all 7 panel components into conditional rendering

**IMPORTANT — vaul API check:** Before writing the Drawer code, read the vaul exports from `node_modules/vaul/`. The research flagged this as LOW confidence. The expected API is `import { Drawer } from "vaul"` with `Drawer.Root`, `Drawer.Trigger`, `Drawer.Portal`, `Drawer.Overlay`, `Drawer.Content`. If the actual API differs, adapt accordingly. If vaul is not installed in the worktree, run `pnpm install` first.

**Mobile section picker** (visible only below lg):

```typescript
import { Drawer } from "vaul";
import { ChevronDown } from "lucide-react";
import { SIDEBAR_ITEMS } from "./settings-sidebar";
```

The mobile picker trigger is a full-width button above the content area, showing the current section's icon and label:

```tsx
{/* Mobile section picker — lg:hidden */}
<div className="lg:hidden mb-4">
  <Drawer.Root open={drawerOpen} onOpenChange={setDrawerOpen}>
    <Drawer.Trigger asChild>
      <button className="w-full flex items-center justify-between px-4 py-3 rounded-[var(--radius-md)] border border-border bg-surface text-sm font-medium">
        <span className="flex items-center gap-2">
          <CurrentIcon className="size-4 text-accent" />
          {currentLabel}
        </span>
        <ChevronDown className="size-4 text-muted" />
      </button>
    </Drawer.Trigger>
    <Drawer.Portal>
      <Drawer.Overlay className="fixed inset-0 bg-black/40" />
      <Drawer.Content className="fixed inset-x-0 bottom-0 rounded-t-[var(--radius-lg)] bg-surface p-4">
        <div className="mx-auto mb-4 h-1.5 w-12 rounded-full bg-muted/30" />
        <p className="text-sm font-semibold mb-3 px-2">Settings</p>
        <div className="space-y-1">
          {SIDEBAR_ITEMS.map((item) => (
            <button
              key={item.key}
              onClick={() => {
                navigate(item.key);
                setDrawerOpen(false);
              }}
              className={cn(
                "w-full flex items-center gap-3 px-4 py-3 text-sm font-medium rounded-[var(--radius-md)] transition",
                activeSection === item.key
                  ? "bg-accent/10 text-accent-strong"
                  : "text-muted-strong hover:bg-black/5"
              )}
            >
              <item.Icon className={cn("size-4", activeSection === item.key ? "text-accent" : "text-muted")} />
              {item.label}
            </button>
          ))}
        </div>
      </Drawer.Content>
    </Drawer.Portal>
  </Drawer.Root>
</div>
```

Add state for drawer: `const [drawerOpen, setDrawerOpen] = useState(false);`

Derive current icon and label from SIDEBAR_ITEMS:
```typescript
const currentItem = SIDEBAR_ITEMS.find((s) => s.key === activeSection) ?? SIDEBAR_ITEMS[0];
const CurrentIcon = currentItem.Icon;
const currentLabel = currentItem.label;
```

**Panel rendering:**

Replace the placeholder `<div>` in the shell with conditional rendering of all 7 panels:

```typescript
import { ProfilePanel } from "./panels/profile-panel";
import { BrandingPanel } from "./panels/branding-panel";
import { BusinessInfoPanel } from "./panels/business-info-panel";
import { GeneralPanel } from "./panels/general-panel";
import { EmailsPanel } from "./panels/emails-panel";
import { IntegrationsPanel } from "./panels/integrations-panel";
import { BillingPanel } from "./panels/billing-panel";

// Inside the return, replace the placeholder div in <main>:
{activeSection === "profile" && <ProfilePanel />}
{activeSection === "branding" && <BrandingPanel />}
{activeSection === "business" && <BusinessInfoPanel />}
{activeSection === "general" && <GeneralPanel />}
{activeSection === "emails" && <EmailsPanel />}
{activeSection === "integrations" && <IntegrationsPanel />}
{activeSection === "billing" && <BillingPanel />}
```

**Import `cn`** from `@/lib/utils` for the mobile picker class conditionals.

Full updated component structure:
1. Imports: vaul Drawer, ChevronDown, cn, SIDEBAR_ITEMS, all 7 panels
2. State: activeSection (existing), drawerOpen (new)
3. Layout: flex container with sidebar (existing) + main content
4. Main content: mobile picker trigger (lg:hidden) + conditional panel rendering
  </action>
  <verify>
    <automated>cd /Users/koss/Desktop/Develop/INV/.claude/worktrees/jolly-euler && grep "Drawer" src/components/app/settings/settings-shell.tsx && grep -c "Panel" src/components/app/settings/settings-shell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - settings-shell.tsx imports from "vaul" (Drawer)
    - settings-shell.tsx contains `Drawer.Root` or equivalent vaul component
    - settings-shell.tsx contains `Drawer.Trigger` with `asChild`
    - settings-shell.tsx contains `lg:hidden` on the mobile picker container
    - settings-shell.tsx contains `ChevronDown` icon in the trigger button
    - settings-shell.tsx contains `"Settings"` heading text in the drawer
    - settings-shell.tsx contains `setDrawerOpen(false)` when a drawer item is clicked
    - settings-shell.tsx imports and renders all 7 panels: ProfilePanel, BrandingPanel, BusinessInfoPanel, GeneralPanel, EmailsPanel, IntegrationsPanel, BillingPanel
    - settings-shell.tsx contains conditional rendering for each panel: `activeSection === "profile"` through `activeSection === "billing"`
    - settings-shell.tsx uses `bg-accent/10` for active drawer item styling (matching desktop)
    - settings-shell.tsx imports `SIDEBAR_ITEMS` from `./settings-sidebar`
  </acceptance_criteria>
  <done>Mobile section picker via Vaul Drawer shows all 7 sections with icons and labels; all panels conditionally rendered; desktop and mobile navigation both functional</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of settings foundation</name>
  <files>none</files>
  <action>Present the completed settings foundation for human review. Start dev server if not running (`pnpm dev`). No code changes in this task.</action>
  <what-built>Complete settings shell with sidebar navigation (desktop), mobile section picker (Vaul Drawer), 7 placeholder panels, keyboard arrow key navigation, URL-synced routing, and per-section SaveButton pattern.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000/app/settings in desktop browser (width 1024px+)
    2. Verify sidebar shows 7 sections: Profile, Branding, Business Info, General, Emails, [separator], Integrations, Billing
    3. Verify Profile is the default active section (highlighted with golden accent background)
    4. Click each sidebar item — verify URL changes to ?section=X and content area updates
    5. Press browser Back button — verify it leaves settings entirely (does not cycle through sections)
    6. Use keyboard Tab to focus a sidebar item, then press Arrow Down/Up — verify focus moves between items
    7. Resize browser to mobile width (below 1024px) — verify sidebar disappears
    8. Verify a full-width section picker button appears showing current section name
    9. Tap the section picker — verify a bottom drawer opens with all 7 sections with icons
    10. Select a different section from the drawer — verify drawer closes and content updates
    11. Verify Integrations panel shows "Third-party integrations are coming soon."
    12. Verify Billing panel shows "Plan and billing management is coming soon."
    13. Verify desktop sidebar "Branding" link in the main app nav (left sidebar) now navigates to /app/settings?section=branding
  </how-to-verify>
  <verify>Human visual review of 13 verification steps above</verify>
  <done>User confirms settings foundation is visually correct and all navigation paths work on desktop and mobile</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- All 7 panel files exist in `src/components/app/settings/panels/`
- Mobile picker opens via Vaul Drawer with all section items
- `grep "settings?section=branding" src/lib/constants.ts` confirms nav update
- `pnpm tsc --noEmit` passes (exit code 0)
- Browser verification: desktop sidebar, mobile picker, keyboard nav, URL sync all functional
</verification>

<success_criteria>
- Mobile users can open section picker and switch sections (NAV-04)
- All 7 panels render with placeholder content
- 5 content panels each have their own disabled SaveButton (A11Y-03)
- Coming soon panels have no SaveButton
- Branding app nav link points to settings section
- All form field labels use Label component (A11Y-01 foundation)
- Visual verification confirms complete settings foundation
</success_criteria>

<output>
After completion, create `.planning/phases/08-settings-foundation/08-03-SUMMARY.md`
</output>
